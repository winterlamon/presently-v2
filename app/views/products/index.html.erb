<h1>Search for a product</h1>

<!-- #<%= form_tag(products_path, method: :get) do %>
#  <%= text_field_tag(:search, params[:search]) %>
#  <%= submit_tag ("Search") %>
#<% end %> -->

<%= form_tag(products_path, method: :get) do %>
<%= select_tag "category_id", options_from_collection_for_select(Category.all, :id, :name) %><br>
<%= select_tag "price", options_for_select([["$0-25"], ["$26-50"], ["$51-75"], ["$76-100"]])%>
<%= submit_tag ("Search") %><br><br><br>
<% end %>

<!-- <p>
  #<%= text_field_tag :search, params[:search] %>
#  <%= select_tag :search_column, options_for_select(Product.column_names, params[:search_column]) %>
#  <%= submit_tag "Search", :name => nil %>
</p> -->


<% @products.each do |product| %>
  <%= link_to image_tag(product.image_url), product.listing_url %>
<% end %>
